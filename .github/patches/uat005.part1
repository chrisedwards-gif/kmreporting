From 6b444a6bffb98c090713b6a0371033dfd2769196 Mon Sep 17 00:00:00 2001
From: Claude <claude@anthropic.local>
Date: Sat, 18 Jul 2026 22:31:03 +0000
Subject: [PATCH] Usability and design pass across reporting surfaces

- Report form: choosing the Sunday auto-fills the Saturday, invalid weeks
  show an inline explanation with a one-click reset to the latest completed
  week, and the sticky bar's vague readiness sentence becomes a live
  six-item checklist that also gates submission on a valid week.
- Dashboard metric cards show sales-weighted group targets and highlight
  food, labour and waste when over target, matching the table's treatment.
- Site performance table renders a dash instead of a dead /reports/<siteId>
  link when a kitchen has no report yet.
- Restored BEM classes for the auth screens and converted remaining inline
  layout styles (app shell topbar, approvals cards, costs grid, report
  detail header, privacy callout icons) into the design system.
- Added a skip-to-content link and decimal keypads on all money/hours
  inputs for mobile entry.
---
 src/app/(app)/approvals/page.tsx              |   6 +-
 src/app/(app)/costs/page.tsx                  |   8 +-
 src/app/(app)/dashboard/page.tsx              |  14 +-
 src/app/(app)/reports/[reportId]/page.tsx     |   4 +-
 src/app/auth/forgot-password/page.tsx         |   8 +-
 src/app/auth/set-password/page.tsx            |   4 +-
 src/app/globals.css                           | 159 +++++++++++++++++-
 src/app/login/page.tsx                        |   4 +-
 src/components/app-shell.tsx                  |   5 +-
 src/components/dashboard/metric-card.tsx      |   5 +-
 .../dashboard/site-performance-table.tsx      |  10 +-
 src/components/reports/approval-form.tsx      |   2 +-
 src/components/reports/report-form.tsx        |  50 +++++-
 13 files changed, 236 insertions(+), 43 deletions(-)
diff --git a/src/app/(app)/dashboard/page.tsx b/src/app/(app)/dashboard/page.tsx
index 8d2e625..ace3cb2 100644
--- a/src/app/(app)/dashboard/page.tsx
+++ b/src/app/(app)/dashboard/page.tsx
@@ -31,6 +31,14 @@ export default async function DashboardPage() {
     })),
   );
   const allStockAdjusted = sites.length > 0 && sites.every((site) => site.foodCostBasis === "stock_adjusted");
+  // Group targets are each kitchen's own target weighted by its share of sales,
+  // so one small site with a loose target cannot mask a large site running hot.
+  const weightedTarget = (selectTarget: (site: (typeof sites)[number]) => number) =>
+    totals.netSales ? sites.reduce((sum, site) => sum + selectTarget(site) * site.netSales, 0) / totals.netSales : 0;
+  const foodTarget = weightedTarget((site) => site.foodCostTarget);
+  const labourTarget = weightedTarget((site) => site.labourTarget);
+  const wasteTarget = weightedTarget((site) => site.wasteTarget);
+  const hasSales = totals.netSales > 0;
 
   return (
     <>
@@ -49,9 +57,9 @@ export default async function DashboardPage() {
 
       <section aria-label="Group metrics" className="metric-grid">
         <MetricCard accent="#2d7a62" label="Net sales" note={`Across ${sites.length} kitchens`} trend="up" value={formatCurrency(totals.netSales)} />
-        <MetricCard accent="#eb6b4f" label={allStockAdjusted ? "Food cost" : "Food cost / spend"} note={`${formatCurrency(totals.cogs)}${allStockAdjusted ? "" : " · mixed basis"}`} value={formatPercentage(foodCostPct)} />
-        <MetricCard accent="#2d7a62" label="Staff cost" note={formatCurrency(totals.staffCost)} value={formatPercentage(labourPct)} />
-        <MetricCard accent="#c78324" label="Waste" note={formatCurrency(totals.wasteCost)} value={formatPercentage(wastePct)} />
+        <MetricCard accent="#eb6b4f" label={allStockAdjusted ? "Food cost" : "Food cost / spend"} note={`${formatCurrency(totals.cogs)} · target ≤ ${formatPercentage(foodTarget)}${allStockAdjusted ? "" : " · mixed basis"}`} overTarget={hasSales && foodCostPct > foodTarget} value={formatPercentage(foodCostPct)} />
+        <MetricCard accent="#2d7a62" label="Staff cost" note={`${formatCurrency(totals.staffCost)} · target ≤ ${formatPercentage(labourTarget)}`} overTarget={hasSales && labourPct > labourTarget} value={formatPercentage(labourPct)} />
+        <MetricCard accent="#c78324" label="Waste" note={`${formatCurrency(totals.wasteCost)} · target ≤ ${formatPercentage(wasteTarget)}`} overTarget={hasSales && wastePct > wasteTarget} value={formatPercentage(wastePct)} />
         <MetricCard accent="#1e2e35" label="Prime cost" note={formatCurrency(totals.cogs + totals.staffCost)} value={formatPercentage(primeCostPct)} />
         <MetricCard accent="#b93f35" label="Review queue" note="Approval blocks sharing" value={`${reviewFlags.length}`} />
       </section>
diff --git a/src/app/(app)/reports/[reportId]/page.tsx b/src/app/(app)/reports/[reportId]/page.tsx
index 7297664..c6a950d 100644
--- a/src/app/(app)/reports/[reportId]/page.tsx
+++ b/src/app/(app)/reports/[reportId]/page.tsx
@@ -48,7 +48,7 @@ export default async function ReportDetailPage({ params }: { params: Promise<{ r
           <h1 className="page-header__title">{report.siteName}</h1>
           <p className="page-header__copy">Submitted by {report.manager}. Financial outputs are current safe aggregates; no pay rates are included.</p>
         </div>
-        <div style={{ display: "flex", gap: ".6rem", flexWrap: "wrap" }}>
+        <div className="page-header__actions">
           <Link className="button button--secondary" href="/reports"><ArrowLeft aria-hidden="true" size={16} /> Reports</Link>
           {report.status === "draft" ? <Link className="button button--primary" href={`/reports/new?report=${report.id}`}><Pencil aria-hidden="true" size={16} /> Continue draft</Link> : null}
         </div>
@@ -106,7 +106,7 @@ export default async function ReportDetailPage({ params }: { params: Promise<{ r
